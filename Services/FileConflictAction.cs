namespace RimWorldModManager.Services
{
    public enum FileConflictAction
    {
        Skip,
        SkipAll,
        Replace,
        ReplaceAll,
        KeepBoth,
        KeepBothAll,
        Cancel
    }
}
