<UserControl x:Class="RimWorldModManager.Views.ModDetailPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:RimWorldModManager.Converters"
             xmlns:local="clr-namespace:RimWorldModManager"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:NullImageConverter x:Key="NullImageConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <local:InvertBooleanToVisibilityConverter x:Key="InvertBooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid>
        <StackPanel x:Name="EmptyState" Visibility="{Binding HasMod, Converter={StaticResource InvertBooleanToVisibilityConverter}}">
            <TextBlock Text="请选择一个 Mod 查看详情" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Margin="16"
                       TextWrapping="Wrap" />
        </StackPanel>

        <Grid x:Name="DetailContent" Visibility="{Binding HasMod, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="16,16,16,0">
                <ui:Button 
                    Click="BackButton_Click"
                    Width="40" Height="40"
                    FontSize="20">
                    ←
                </ui:Button>
                <TextBlock 
                    Text="Mod 详情" 
                    FontSize="18" 
                    FontWeight="Bold" 
                    VerticalAlignment="Center"
                    Margin="8,0,0,0" />
            </StackPanel>

            <Border Grid.Row="1" Margin="16,16,16,0" BorderThickness="1">
                <Image 
                    Width="260" 
                    Height="180" 
                    Stretch="UniformToFill">
                    <Image.Source>
                        <MultiBinding Converter="{StaticResource NullImageConverter}">
                            <Binding Path="Mod.PreviewImagePath" />
                        </MultiBinding>
                    </Image.Source>
                </Image>
            </Border>

            <StackPanel Grid.Row="2" Margin="16,16,16,0">
                <TextBlock Text="Mod 名称" FontSize="12" />
                <TextBlock Text="{Binding Mod.Name}" FontSize="16" FontWeight="Bold" Margin="0,4,0,0" />
            </StackPanel>

            <StackPanel Grid.Row="3" Margin="16,8,16,0">
                <TextBlock Text="作者" FontSize="12" />
                <TextBlock Text="{Binding AuthorDisplay}" FontSize="14" Margin="0,4,0,0" />
            </StackPanel>

            <StackPanel Grid.Row="4" Margin="16,8,16,0">
                <TextBlock Text="版本" FontSize="12" />
                <TextBlock Text="{Binding VersionDisplay}" FontSize="14" Margin="0,4,0,0" />
            </StackPanel>

            <StackPanel Grid.Row="5" Margin="16,8,16,0">
                <TextBlock Text="最后更新" FontSize="12" />
                <TextBlock Text="{Binding LastUpdatedDisplay}" FontSize="14" Margin="0,4,0,0" />
            </StackPanel>

            <StackPanel Grid.Row="6" Margin="16,8,16,0">
                <TextBlock Text="标签" FontSize="12" />
                <TextBlock Text="{Binding TagsDisplay}" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap" />
            </StackPanel>

            <StackPanel Grid.Row="7" Margin="16,8,16,16">
                <TextBlock Text="描述" FontSize="12" />
                <TextBlock Text="{Binding DescriptionDisplay}" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
