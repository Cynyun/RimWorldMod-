<ui:FluentWindow x:Class="RimWorldModManager.Views.BatchImportOptionsDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="批量导入选项" Height="550" Width="520"
                 WindowStartupLocation="CenterOwner"
                 MouseDown="FluentWindow_MouseDown">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="批量导入设置" FontSize="20" FontWeight="Bold" Margin="0,0,0,8" />
            <TextBlock x:Name="ModCountText" TextWrapping="Wrap" Margin="0,0,0,8" FontSize="14" />
            <TextBlock x:Name="ConflictCountText" TextWrapping="Wrap" Margin="0,0,0,8" FontSize="14" Foreground="#F6213C" Visibility="Collapsed" />
        </StackPanel>

        <Separator Grid.Row="1" Margin="0, 4" />

        <StackPanel Grid.Row="2">
            <TextBlock Text="之后遇到同名 Mod 时的处理方式：" FontWeight="SemiBold" Margin="0,0,0,8" FontSize="14" />
            
            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" 
                    CornerRadius="4" 
                    Padding="12" 
                    Margin="0,0,0,8">
                <StackPanel>
                    <RadioButton x:Name="AskEachRadio" 
                                 Content="逐个询问" 
                                 IsChecked="True" 
                                 FontSize="14"
                                 Margin="0,0,0,4"/>
                    <TextBlock Text="对每个重名 Mod 逐一询问处理方式" 
                               Foreground="Gray" 
                               Margin="24,0,0,0"
                               FontSize="12"/>
                </StackPanel>
            </Border>

            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" 
                    CornerRadius="4" 
                    Padding="12" 
                    Margin="0,0,0,8">
                <StackPanel>
                    <RadioButton x:Name="ReplaceAllRadio" 
                                 Content="全部替换" 
                                 FontSize="14"
                                 Margin="0,0,0,8"/>
                    <TextBlock Text="删除所有已存在的同名 Mod，使用新版本替换" 
                               Foreground="Gray" 
                               Margin="24,0,0,0"
                               FontSize="12"/>
                </StackPanel>
            </Border>

            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" 
                    CornerRadius="4" 
                    Padding="12" 
                    Margin="0,0,0,8">
                <StackPanel>
                    <RadioButton x:Name="KeepBothAllRadio" 
                                 Content="全部保留" 
                                 FontSize="14"
                                 Margin="0,0,0,8"/>
                    <TextBlock Text="为所有新 Mod 创建重命名副本（如 Mod (1)、Mod (2)）" 
                               Foreground="Gray" 
                               Margin="24,0,0,0"
                               FontSize="12"/>
                </StackPanel>
            </Border>

            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" 
                    CornerRadius="4" 
                    Padding="12" 
                    Margin="0,0,0,8">
                <StackPanel>
                    <RadioButton x:Name="SkipAllRadio" 
                                 Content="全部跳过" 
                                 FontSize="14"
                                 Margin="0,0,0,8"/>
                    <TextBlock Text="跳过所有重名的 Mod，只导入不冲突的文件" 
                               Foreground="Gray" 
                               Margin="24,0,0,0"
                               FontSize="12"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:Button Grid.Column="1" 
                       Content="开始导入" 
                       Click="StartButton_Click"
                       Margin="0,0,8,0"
                       Appearance="Primary"/>
            <ui:Button Grid.Column="2" 
                       Content="取消" 
                       Click="CancelButton_Click" />
        </Grid>
    </Grid>
</ui:FluentWindow>
