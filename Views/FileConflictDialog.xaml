<ui:FluentWindow x:Class="RimWorldModManager.Views.FileConflictDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="文件冲突" Height="320" Width="500"
                 WindowStartupLocation="CenterOwner"
                 MouseDown="FluentWindow_MouseDown">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="检测到同名 Mod" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" />
            <TextBlock x:Name="ModNameText" TextWrapping="Wrap" Margin="0,0,0,16" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Hidden" 
                      HorizontalScrollBarVisibility="Hidden"
                      Margin="0,8,0,16">
            <StackPanel>
                <TextBlock Text="选择如何处理此冲突：" FontWeight="SemiBold" Margin="0,0,0,12" />

                <RadioButton x:Name="ReplaceRadio" Content="替换现有文件" Margin="0,0,0,8" IsChecked="True" />
                <RadioButton x:Name="KeepBothRadio" Content="保留两者（重命名新文件）" Margin="0,0,0,8" />
                <RadioButton x:Name="SkipRadio" Content="跳过此文件" Margin="0,0,0,8" />

                <CheckBox x:Name="ApplyToAllCheck" Content="对所有冲突执行此操作" Margin="0,16,0,0" Visibility="Collapsed" />
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:Button Grid.Column="1" 
                       Content="确定" 
                       Click="ConfirmButton_Click"
                       Margin="0,0,8,0" />
            <ui:Button Grid.Column="2" 
                       Content="取消" 
                       Click="CancelButton_Click" />
        </Grid>
    </Grid>
</ui:FluentWindow>
