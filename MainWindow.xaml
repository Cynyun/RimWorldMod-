<ui:FluentWindow x:Class="RimWorldModManager.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:local="clr-namespace:RimWorldModManager"
                 mc:Ignorable="d"
                 Title="RimWorld Mod 管理器" Height="600" Width="800"
                 WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="RimWorld Mod 管理器" />

        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ui:TextBox Grid.Column="0" 
                            x:Name="WorkshopIdTextBox" 
                            PlaceholderText="输入 Workshop ID"
                            Margin="0,0,8,0" />

                <ui:Button Grid.Column="1" 
                           x:Name="DownloadButton" 
                           Content="下载 Mod"
                           Click="DownloadButton_Click"
                           Margin="0,0,8,0">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="Download24" />
                    </ui:Button.Icon>
                </ui:Button>

                <ui:ProgressRing Grid.Column="2" 
                                 x:Name="DownloadProgress" 
                                 IsIndeterminate="{Binding IsLoading}" 
                                 Width="24" Height="24" />
            </Grid>

            <ui:DataGrid Grid.Row="1" 
                         x:Name="ModsDataGrid"
                         ItemsSource="{Binding Mods}"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Workshop ID" 
                                           Binding="{Binding WorkshopId}" 
                                           Width="120" />
                    <DataGridTextColumn Header="Mod 名称" 
                                           Binding="{Binding Name}" 
                                           Width="*" />
                    <DataGridTemplateColumn Header="启用">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ui:ToggleSwitch IsChecked="{Binding IsEnabled}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </ui:DataGrid>
        </Grid>


    </Grid>
</ui:FluentWindow>